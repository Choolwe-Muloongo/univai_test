# Firebase Studio

This is a NextJS starter in Firebase Studio.

To get started, take a look at src/app/page.tsx.

Flutter version of the app lives in `../flutter_app/` (see `../flutter_app/README_FLUTTER.md`).
